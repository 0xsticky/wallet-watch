{
  "name": "wallet-watch",
  "version": "0.0.1",
  "description": "A utility for watching crypto wallets and getting notifications when a transaction occurs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "prettier": "npx prettier --write .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "0xSticky",
  "license": "ISC",
  "dependencies": {
    "ethers": "^5.5.3"
  },
  "devDependencies": {
    "eslint": "^8.7.0",
    "prettier": "2.5.1"
  }
}
